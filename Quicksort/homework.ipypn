def quicksort(list):
    left = []
    right = []
    middle = []
    
    if len(list)>1:
        
        pivot = a[0]
        for i in list:
            if i < pivot:
                left.append(i)
            elif i == pivot:
                middle.append(i)
            else:
                right.append(i)
    else:
        return list

    return quicksort(left) + middle + quicksort(right)
    
    list = [1,5,7,3,9,100,53,8]
 
 quciksort(list)
